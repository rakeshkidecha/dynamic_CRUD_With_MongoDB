<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
    <!-- remix icon  -->
    <link rel="stylesheet" href="/remix/remixicon.css" type="text/css">
    <!-- font family -->
    <link rel="stylesheet" href="/fonts/stylesheet.css" type="text/css">
    <!-- bootstrap css  -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
    <!-- main css  -->
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <main class="w-100" style="padding: 40px 0;">
        <h2 class="text-center mb-3">Update User</h2>
        <form method="post" action="/editUser" class="py-3 px-4 mx-auto" style="width:40%;">
            <input type="hidden" name="uId" value="<%= singleUser.id %>">

            <label class="mt-2 mb-1">Enter First Name:- </label>
            <input type="text" name="fName" value="<%= singleUser.fName %>" class="form-control bg-transparent py-1"/>

            <label class="mt-2 mb-1">Enter Last Name:- </label>
            <input type="text" name="lName" value="<%= singleUser.lName %>" class="form-control bg-transparent py-1"/>

            <label class="mt-2 mb-1">Enter Email:- </label>
            <input type="email" name="email" value="<%= singleUser.email %>" class="form-control bg-transparent py-1"/>

            <label class="mt-3 mb-1 w-100">Choose Gender:- </label>
            <input type="radio" name="gender" value="Male" class="me-1" <%= singleUser.gender==='Male'?'checked':'' %>/>Male
            <input type="radio" name="gender" value="Female" class="me-1 ms-3" <%= singleUser.gender==='Female'?'checked':'' %>/>Female
            <input type="radio" name="gender" value="Other" class="me-1 ms-3" <%= singleUser.gender==='Other'?'checked':'' %>/>Other

            <label class="mt-3 mb-1 w-100">Check your hobby:- </label>
            <% let hobbyNames=["Reading","Walking","Playing","Singing","writing"] %>
            <div class="row">
                <% hobbyNames.map((item)=>{ %>
                    <div class="col-lg-3 d-flex">
                        <input type="checkbox" name="hobby" value="<%= item %>" <%= singleUser.hobby.includes(item)?'checked':'' %>/><span class="ms-2"><%= item %></span>
                    </div>
                <% }) %>
            </div>

            <label class="mt-2 mb-1">Select City:- </label>
            <select name="city" class="bg-transparent">
                <% let cityNames=["Surat","Rajkot","Amreli"] %>
                <option value="">--select--</option>
                <% cityNames.map((item)=>{ %>
                    <option value="<%= item %>" <%= singleUser.city===item?'selected':'' %> ><%= item %></option>
                <% }) %>
            </select>

            <label class="mt-2 mb-1 w-100">About:- </label>
            <textarea name="about" class="form-control bg-transparent py-1"><%= singleUser.about %></textarea>

            <div class="text-center  mt-4">
                <button type="submit" class="btn me-2">Confirm</button>
                <a href="/" class="btn">cancel</a>
            </div>
            
        </form> 
    </main>

    <script src="/assets/js/bootstrap.bundle.min.js" type="text/javascript"></script>
</body>
</html>